<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.attachments.services.domain.dao.QueryAttachmentsDao">
    <sql id="Base_Column_List">
            attachments.*
    </sql>

        <sql id="QueryOwnedOrgIds">
            SELECT children.id FROM t_sys_org, t_sys_org as children WHERE t_sys_org.left_num &lt;= children.left_num AND
            t_sys_org.right_num >= children.left_num and t_sys_org.id=#{ownerOrgId} order by t_sys_org.node_level ASC
        </sql>

                                                                                                                                                                   <select id="findAttachmentsPage" resultType="AttachmentsRecord" parameterType="AttachmentsRecord">
      SELECT attachments.*
        FROM attachments
        <if test="record.orgId > 0">
            ,(<include refid="QueryOwnedOrgIds"></include>) AS ownedOrgIds
        </if>
        WHERE 1=1
        <if test="record.orgId > 0">
             AND t_sys_user.org_id = ownedOrgIds.id
        </if>
                                <if test="record.id != null and record.id>0 ">
            AND attachments.id LIKE CONCAT('%',#{record.id},'%')
            </if>
                                <if test="record.name != null and record.name!= ''">
            AND attachments.name LIKE CONCAT('%',#{record.name},'%')
            </if>
                                <if test="record.url != null and record.url!= ''">
            AND attachments.url LIKE CONCAT('%',#{record.url},'%')
            </if>
                                    
                                                                                         <if test="record.tableName != null and record.tableName!= ''">
            AND attachments.table_name LIKE CONCAT('%',#{record.tableName},'%')
            </if>
                                    
                                                                                         <if test="record.referenceId != null and record.referenceId>0 ">
            AND attachments.reference_id LIKE CONCAT('%',#{record.referenceId},'%')
            </if>
                        <!--
        <if test="search != null and search != ''">
            OR attachments.name LIKE CONCAT('%',#{search},'%')
        </if>
        -->
    </select>
</mapper>